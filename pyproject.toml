[project]
name = "trading-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "langchain-core>=0.3",
    "pandas>=2.0",
    "pandas-ta>=0.3.14b",
    "requests>=2.31",
    "yfinance>=0.2",
    "alpaca-py>=0.29",
    "apscheduler>=3.10",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agents", "broker", "scanner"]

[tool.pytest.ini_options]
testpaths = ["tests"]
